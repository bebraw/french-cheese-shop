{
  "name": "presentation-template",
  "version": "0.1.0",
  "private": true,
  "description": "Teaching presentation generator with native PDF output.",
  "scripts": {
    "build": "node generator/compile.js",
    "build:all": "npm run build",
    "archive:update": "npm run build && cp slides/output/teaching-proof-ai-re.pdf archive/teaching-proof-ai-re.pdf",
    "validate:geometry": "node generator/validate-geometry.js",
    "validate:text": "node generator/validate-text.js",
    "validate": "npm run validate:geometry && npm run validate:text",
    "baseline:render": "npm run build && node generator/update-render-baseline.js",
    "validate:render": "npm run build && node generator/validate-render.js",
    "validate:all": "npm run validate && npm run validate:render",
    "quality:gate": "npm run validate && npm run validate:render"
  },
  "dependencies": {
    "pdfkit": "0.18.0",
    "pptxgenjs": "^3.12.0",
    "qrcode": "1.5.4"
  }
}
